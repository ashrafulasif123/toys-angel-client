import React from 'react';
import useTitle from '../../hooks/useTitle';

const Blogs = () => {
    useTitle('Blog')
    return (
        <div>
            This is the blog page
        </div>
    );
};

export default Blogs;